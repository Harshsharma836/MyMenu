import React from 'react';

export const metadata = {
  title: 'Login - MyMenu',
  description: 'Login to MyMenu',
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
