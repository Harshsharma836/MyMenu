import React from 'react';

export const metadata = {
  title: 'Digital Menu - MyMenu',
};

export default function MenuLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
