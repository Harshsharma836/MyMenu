import React from 'react';

export const metadata = {
  title: 'Restaurant - MyMenu',
};

export default function RestaurantLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
